<% @comments.each do |comment| %>
  <%= comment.description %>
  <%= link_to "Edit Comment", edit_image_comment_path(@image, comment) %>
  <%= link_to "Delete", image_path(@image, comment), method: :delete,
  data: { confirm: "Are you sure?" } %><br>
<% end %>
