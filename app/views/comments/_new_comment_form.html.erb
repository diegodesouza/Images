<%= form_for [@image, @comment] do |f| %>
<div class="field">
  <%= f.label :description %>
  <%= f.text_field :description %>
</div>
<div class="action">
  <%= f.submit "Submit Comment", class:"button", method: :post %>
</div>
<% end %>
